#include <stdio.h>

/**
 * Calculates the `index`th Fibonacci number, zero-indexed.
 */
int fibonacci(int index)
{









}

int main(void)
{
    // should print 8
    printf("%i\n", fibonacci(5));

    // should print 1
    printf("%i\n", fibonacci(1));

    // should print 1
    printf("%i\n", fibonacci(0));
}